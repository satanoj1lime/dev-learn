// #include <stdio.h>

/*
char *get_temperature_status(int temp){
  if (temp <= 50){
    printf("too cold");
  } else if (temp >= 90){
    printf("too hot");
  } else {
    printf("just right");
  }
}
*/
/*
char *get_temperature_status(int temp){
    if (temp <= 70) {
        return "too cold";
    } else if (temp >= 90) {
        return "too hot";
    } else {
        return "just right";
    }
}

int main(void){
    const char *result = get_temperature_status(90);
    printf("%s\n", result);
    return 0;
}
*/
