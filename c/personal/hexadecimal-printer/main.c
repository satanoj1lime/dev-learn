#include <stdio.h>


int main(){
// Convert the char to its ASCII value (e.g. Z = 90)
    
// Divide by 16 this is the first part of the hex (e.g. 90/16 = 5) 
// The remainder is the second part of the hex (e.g. mod(90,16) = 10)
// The hex value for 10 is A, so the hex for Z is 5A 
    return 0;
}
